// NLP helper — parseIntent() is the only public symbol. Importable at
// `@satrank/sdk/nlp`. EN-only in SDK 1.0 (Phase 6); FR deferred to 6bis.
export { parseIntent } from './parseIntent';
export type { ParseIntentOptions, ParsedIntent } from './parseIntent';
